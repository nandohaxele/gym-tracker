"""Cross-cutting concerns shared across all modules.

Contains: config (settings), database (engine/session), security (hash + JWT),
dependencies (auth dependency), response envelope, and custom exceptions.
"""
