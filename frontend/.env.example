# API base URL the Axios client will use.
# In dev, Vite also proxies /api -> http://localhost:8000 (see vite.config.js),
# so you can leave this as the relative path "/api" if you prefer.
VITE_API_BASE_URL=http://localhost:8000/api
